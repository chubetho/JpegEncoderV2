{
  "name": "jpegencoder",
  "type": "module",
  "module": "src/index.ts",
  "scripts": {
    "test": "bun test --watch",
    "test:update": "bun test --update-snapshots --watch",
    "lint": "eslint . --fix",
    "build": "bun build --entry src/index.ts --target bun --minify --outdir ./dist"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^2.6.2",
    "@types/bun": "latest",
    "eslint": "^8.56.0"
  }
}
