{
  "name": "jpegencoder",
  "type": "module",
  "module": "src/index.ts",
  "scripts": {
    "start": "bun src/index.ts",
    "test": "bun test --watch",
    "test:update": "bun test --update-snapshots",
    "lint": "eslint . --fix",
    "build": "bun build src/index.ts --compile --minify --outfile group5"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^2.4.5",
    "bun-types": "latest",
    "eslint": "^8.55.0"
  }
}
